name: "CI - Nix"
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
jobs:
  nix:
    runs-on: "${{ matrix.os }}-latest"
    strategy:
      matrix:
        os: [ubuntu, macos]
        #extends: ["", "pkgs-eigen5", "pkgs-full", "pkgs-nanobind"]
        extends: [""]
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/install-nix-action@v31
      - uses: cachix/cachix-action@v17
        if: github.repository == 'ROLKNEEMATICS/panda_prosthesis' && github.ref == 'refs/heads/main'
        with:
          name: mc-rtc-nix
          # XXX: disable push for now, will need to see what to do about private repositories
          # authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
          extraPullNames: "ros,gepetto,mc-rtc-nix"
      - run: nix flake check -L
      - run: nix build -L ".#${{ matrix.extends }}"
  check:
    if: always()
    name: check-macos-linux-nix
    runs-on: ubuntu-latest
    needs:
      - nix
    steps:
      - uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
